if theme.newest_comments.enable && !['Livere','Facebook Comments','Giscus'].includes(theme.comments.use[0])
  section.card-widget#card-newest-comments(aria-label=_p('aside.card_newest_comments.headline'))
    h3.item-headline
      i.fas.fa-comment-dots(aria-hidden="true")
      span= _p('aside.card_newest_comments.headline')
    .aside-list(role="list")
      p.loading-text(aria-live="polite")= _p('aside.card_newest_comments.loading_text')
